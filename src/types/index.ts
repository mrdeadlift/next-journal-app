export interface Task {
  id: number
  content: string
  completed: boolean
}

export interface JournalEntry {
  gratitude: string
  futureVision: string
  actions: string
  tasks: string
  bulletJournal: string
}
